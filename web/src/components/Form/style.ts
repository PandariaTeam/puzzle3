import { css } from '@emotion/css';

export const labelCls = css`
  color: rgba(0, 0, 0, 0.88);
  font-weight: 600;
  font-size: 14px;
  margin: 0;
`;

export const titleCls = css`
  margin: 0;
  cursor: pointer;
  &-text {
    margin-right: 10px;
  }
`;
